{
  "name": "package.json",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "heroku-postbuild": "npm run build",
    "start": "serve -s dist -l $PORT"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
